# SquareSumV1-S02AY 云端验证与阶段测评交接

更新时间：2026-08-05 16:10（Asia/Shanghai）

## 1. 结论

S02AY 已在昇腾 910B4、CANN 社区版 8.5.0 环境完成构建、安装、正确性
回归和代表性性能测试，可作为下一轮官方平台测评包。它仍是“阶段候选”，
在收到 Case1～Case5 的官方耗时前，不宣称进入前十，也不替换冻结的正式
基线源码和旧 ZIP。

本轮不是针对隐藏 shape 硬编码。新增路由和同步都由合法公开约束内的边界
矩阵发现，并对 FP16、BF16、FP32 交叉验证。

## 2. 候选与构建产物

源码候选：

```text
candidates/squaresum_s02ay_workspace_task_boundary_20260805/SquareSumV1
```

云端实验：

```text
/home/ma-user/work/s9/experiments/squaresum_s02ay_cloud_20260805_1603
```

云端 `.run`：

```text
大小：593974 bytes
SHA-256：11066D7CCC6339B904FC052F3C21E90B856E3448838564A7B6E0771B1E18D3F5
```

阶段测评 ZIP：

```text
D:\29722\Desktop\GCC\提交相关材料\20260805\S02AY\SquareSumV1.zip
大小：591289 bytes
SHA-256：C5A9F237124374D97C78DAD97966833AD6A5783EA9B6C8D67CD53ECDEBE162AC
```

ZIP 内目录仍为官方上传使用的 `SquareSumV1_zip/`，只包含 `op_host/`、
`op_kernel/` 和 `custom_opp_euleros_aarch64.run`。包内三份主要源码与
S02AY 候选逐文件 SHA-256 一致，包内 `.run` 与云端已验证二进制一致。

## 3. 本轮发现并修复的问题

1. grouped vector8 在一个核连续处理第二个任务时缺少 V→MTE2 依赖，
   导致后续任务输入覆盖；S02AT 补齐任务边界。
2. 末轴长归约的多输出二维搬运在超长 source gap 上不安全；S02AU 对
   2/4/8 输出快路增加步长门控，宽度 1 保留，并修复长向量跨 tile/任务
   的缓冲区复用边界。
3. fastPath4 与 fastPath2 在一个核跨越多个输出段时缺少 V→MTE2 依赖；
   S02AV 补齐输出段边界。
4. grouped 短尾维的对齐专用实现对固定宽度 1/2/4 会触发 AICore 异常；
   S02AX 仅将窄宽对齐子路径路由到已验证的 flat-rows 实现，宽度 8 快路
   不变。
5. workspace 中间归约完成一个输出块后复用输入 UB 时缺少任务边界，
   常数输入出现 125/155 个错误；S02AY 补齐边界后，常数与随机矩阵恢复。

失败的 S02AS、S02AW 等诊断候选均保留作证据，但没有进入阶段测评包。

## 4. 正确性证据

S02AY 安装后得到以下独立汇总：

```text
扩展边界与随机矩阵：726/726
定向矩阵：46/46
随机矩阵：150/150
BF16 严格语义：4/4
关键性能路径正确性门：45/45
workspace 中间归约专项：6/6
grouped 对齐窄宽专项：9/9
中间大步长专项：6/6（由 S02AV 验证，后续改动未触及该路径）
```

关键故障复现 `(31,129,33,5)`、轴 `(1,2)`：

- 修复前，FP16 常数 0.5 的 155 个输出中有 125 个错误，最大绝对误差
  38；
- 修复后，FP16/BF16 常数和随机结果逐元素一致；
- 修复后，FP32 随机结果最大绝对差 0.001953125，允许上限约 0.2389。

## 5. 代表性性能

测量方式为 NPU Event，多轮预热后取 5 轮中位数。它只用于比较通用路径，
不能代替官方隐藏 Case。

| 路径 | 形状/类型 | S02AY 中位数（μs） |
| --- | --- | ---: |
| 公开代表 | `(123,31)` FP16 末轴 | 27.186 |
| 末轴 | `(1024,1024)` FP16 | 31.917 |
| 中间轴 | `(64,257,65)` FP16 | 32.637 |
| 多轴 | `(8,33,17,65)` FP16 | 26.821 |
| 三重多轴 | `(8,17,9,33,65)` FP16 | 64.828 |
| 跨步树归约 | `(32,17,64,65)` FP16 | 82.086 |
| 跨步树归约 | `(32,17,64,65)` BF16 | 85.354 |
| 跨步树归约 | `(32,17,64,65)` FP32 | 77.010 |
| 大前缀 | `(200,1000,64)` FP16 | 72.513 |
| 大全归约 | `(200,1000,64)` FP16 | 34.221 |
| workspace 8 输出 | `(8,4,8192)` FP16 | 28.866 |
| 末轴 N=10000 | `(64,10000)` FP16 | 31.664 |

33 个代表性基准全部通过；多数路径为 24～33 μs。较慢的三类仍是后续
优化重点：三重多轴、跨步树归约和大前缀归约。

## 6. 官方评测回传格式

请提交本报告第 2 节所列的 S02AY ZIP，并完整回传：

```text
Case1: Pass, Result: ...
Case2: Pass, Result: ...
Case3: Pass, Result: ...
Case4: Pass, Result: ...
Case5: Pass, Result: ...
prof_sum: ...
```

若任一 Case 为 `Run failed` 或 `Incorrect`，同时保存平台结果文本和提交
时间。每一道题独立排名；SquareSumV1 当前第 10 名参考门槛仍为
1268.598 μs。收到结果后按单 Case 差值选择下一条通用优化路径。

## 7. 继续工作边界

- 当前冻结正式源码和 2026-07-29 旧包不覆盖，直到 S02AY 官方结果返回；
- 不猜测隐藏 shape、dtype 或轴组合；
- 任何后续候选先复制 S02AY 到新目录，只改变一个可测假设；
- 云端每轮日志保存在独立实验目录，本地 `artifact/` 只保留最近一次命令；
- `.run`、ZIP、SSH 私钥和云端日志不提交 GitHub；候选源码、诊断脚本、
  报告与最新 `artifact/` 可以提交 main。
