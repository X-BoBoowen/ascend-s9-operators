# SquareSumV1-S02CN 云端验证与待测评交接

更新时间：2026-08-06（Asia/Shanghai）

## 1. 结论

S02CM 正式结果为：

```text
6.550 / 400.108 / 139.1025 / 2546.821 / 895.208
prof_sum = 3987.7895 us
```

它比 S02BA 基线 `3985.330 us` 慢 `2.4595 us`（`+0.0617%`），比
S02CL 的 `3986.639 us` 慢 `1.1505 us`。变化属于平台波动量级且方向
为退化，S02CM 正式淘汰。

S02CN 不再微调 fastPath4，而是修复 fastPath2 连续中间轴归约的通用
调度和搬运结构。在 Ascend 910B4、CANN 社区版 8.5.0 上完成：

```text
新增边界、rank 与三 dtype 正确性       96/96
此前七组累计正确性回归               315/315
16M fastPath2 宽度矩阵                 48/48
32M 大规模全 fastPath 图谱             26/26
4M 小规模全 fastPath 图谱              42/42
正式 ZIP 解包、安装后专项复验          96/96
云端发布包源码/RUN/权限审计              PASS
```

S02CN 尚无正式平台结果。公开真机结果证明算法路径本身有结构性收益，但
不能证明隐藏 Case 的 shape、dtype 或 TilingKey，也不能替代正式五 Case
测评。

## 2. 根因与实现

fastPath2 表示被归约轴位于连续内存布局的中间，逻辑输入可折叠为：

```text
[output rows, reduce rows, inner elements]
```

旧实现按单个输出元素分核。一个核的工作边界可能落在 inner 行中间，随后
对每个归约行发起短 DMA、逐行补齐到 32 字节，并按 padded stride 做归约。
当 inner 较小或不对齐时，命令数量和补齐流量远高于有效数据量，公开 16M
矩阵出现 `inner=15/17` 超过 1 ms 的结构性低吞吐。

S02CN 新增 TilingKey 15，把完整输出行作为最小调度任务：

- 每个任务固定处理一个完整 inner 输出行；
- 一个 tile 将 `reduce rows × inner` 合并为一次连续 DMA；
- 只在整个 tile 尾部做一次 padding；
- 复用任意行数的分块树归约，将部分和规约到 8 行；
- 每个输出元素只执行一次 8 行最终合并；
- 不使用 workspace，不改变 FP16、BF16、FP32 的既有平方语义。

通用路由条件为：fastPath2、输入不少于 `2^20` 元素、归约长度不少于
512、inner 为 9～129、完整输出行不少于 40。条件只依赖公开 shape、
axis、布局和 UB 容量；门限外输入严格回退已有实现，不包含 Case 编号或
隐藏数据硬编码。

## 3. 正确性覆盖

新增 96 项专项测试覆盖三种 dtype，并逐项跨越：

- inner 下界与上界：8/9、15/16/17、31/32/33、63/64/65、
  127/128/129/130；
- 输出行数：39/40/41，以及不能被 40 核整除的 79/83；
- 归约长度：511/512、4031/4033；
- 输入规模：`2^20` 元素门限的上下两侧；
- rank-4/rank-5、扁平外层、`keep_dims`、负轴和无序轴；
- 中间含单例维但物理连续的多轴归约。

随后从头运行 S02BT、S02CA、S02CB、S02CE、S02CF、S02CG 和 S02CL
七套历史测试，结果为 `315/315`。正式 ZIP 解包并安装其中 RUN 后，专项
测试再次通过 `96/96`。

## 4. 性能证据

16M 元素 fastPath2 公共矩阵中，典型相邻宽度结果如下：

| inner | dtype | S02CM（us） | S02CN（us） | 加速比 |
| ---: | --- | ---: | ---: | ---: |
| 15 | FP16 | 1362.37 | 79.81 | 17.07x |
| 15 | FP32 | 1107.66 | 78.61 | 14.09x |
| 17 | FP16 | 1679.00 | 78.38 | 21.42x |
| 17 | FP32 | 1371.07 | 79.78 | 17.19x |
| 31 | FP16 | 792.38 | 80.87 | 9.80x |
| 32 | FP32 | 299.09 | 80.09 | 3.73x |
| 64 | FP16 | 167.12 | 82.02 | 2.04x |
| 65 | FP32 | 357.16 | 82.62 | 4.32x |
| 127 | FP16 | 211.01 | 85.05 | 2.48x |
| 129 | FP32 | 198.01 | 95.74 | 2.07x |

相同 32M 大规模图谱的 fastPath2、`inner=64` 点：

```text
FP16: 299.660 -> 107.670 us（2.783x，623.3 GB/s）
FP32: 404.460 -> 211.650 us（1.911x，634.1 GB/s）
```

4M 图谱中，该新路径已接近算子启动开销，`inner=64` 为
`59.927/60.407 us`（FP16/FP32）；rank-4 fastPath2 为
`65.940/65.147 us`。未命中路由仍使用旧 TilingKey，其变化处于运行噪声
范围。

## 5. 路径、提交包与哈希

本地候选源码：

```text
candidates/squaresum_s02cn_middle_full_rows_20260806/SquareSumV1
```

云端实验与发布快照：

```text
/home/ma-user/work/s9/experiments/squaresum_s02cn_middle_full_rows_20260806_1845
/home/ma-user/work/s9/releases/squaresum_s02cn_20260806_1910
```

本地提交包：

```text
D:\29722\Desktop\GCC\提交相关材料\20260806\S02CN\SquareSumV1.zip
大小：877998 bytes
ZIP SHA-256：1D04777575176A168860CDA1BE96B2F75779C7FAAEF401E78DFA4F8845D28E16
RUN SHA-256：4E72EA2372B9C94719610D1343C8E1D4B6F1B220AC7A68B5C61497EA85A778B3
Host SHA-256：3112CFF048E10831F2FABF9F04D62178D16C537465678FFD0211629E55BE2BCD
Kernel SHA-256：3E0B125762D8E0C007D47F9939F6986F304BED78AB801D05A3AEE28FEEA027C4
```

ZIP 只有顶层 `SquareSumV1_zip/`，共 9 个目录/文件条目；源码权限为
`0640`，RUN 为 `0755`。包内五份源码和 RUN 与已验证构建目录逐字节一致。

## 6. 正式结果边界

```text
S02BA baseline = 3985.330 us
S02CL           = 3986.639 us（淘汰）
S02CM           = 3987.7895 us（淘汰）
S02CN           = 尚待正式结果
目标            = 1195.599 us
```

应上传 S02CN 包，并以五个 Case 全部 Pass 后的逐 Case 耗时和
`prof_sum` 判断是否保留。不得根据本报告的公共矩阵反推或硬编码隐藏输入。
