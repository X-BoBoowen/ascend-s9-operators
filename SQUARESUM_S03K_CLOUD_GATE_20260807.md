# SquareSumV1 S03K 云端性能与正确性门禁

日期：2026-08-07（Asia/Shanghai）

## 1. 结论

S03K 的内部候选晋级门禁曾通过，但正式平台结果证明该路线没有命中
隐藏评分主路径，现已淘汰：

```text
Case1: 7.692
Case2: 402.132
Case3: 242.372
Case4: 1721.900
Case5: 903.044
prof_sum: 3277.140
```

相对上一正式版本 S03J 的 `3259.544 μs` 回退 `17.596 μs（0.5398%）`；
相对历史最好 S02F 的 `3223.995 μs` 回退 `53.145 μs（1.6484%）`。
五个 Case 均比 S03J 慢，因此 S03K 不再作为推荐基线。以下门禁数据仅保留
为可追溯的实验记录，不能替代正式结果：

- 目标路径 A/B/A：15/15 正确，聚合提升 **84.7826%**；
- 目标路径最小单点提升：**50.8800%**；
- 两轮基线最大漂移：**2.2591%**；
- 未修改控制路径：15/15 正确，最大表观回退 **2.0023%**；
- 完整与边界正确性：**1130/1130**；
- 静态路由审计：正例 4/4、反例 5/5。

这些数字是 Ascend 910B/CANN 8.5.0 上的公开、通用形状验证，不是正式
隐藏 Case 成绩，也不据此推断隐藏数据。

## 2. 改动与根因

S03J 的完整图谱显示，`fastPath2` 的大规模小 inner 归约只有约
2.6～12.6 GB/s。旧 workspace split-K 会让多个核反复遍历同一批输出
行，并为每一行发起短 2-D DMA，调度与同步成本远高于计算本身。

S03K 只在以下通用条件成立时启用 tiling key 6：

- `fastPath == 2`；
- 输入元素数不少于 1,048,576；
- `reduceElements >= 2048`；
- `1 <= innerElements <= 8`；
- 完整输出行数不少于 40。

新路径把完整 `[reduce, inner]` 行归属给单个核，以连续单块 DMA 分片搬运，
在 UB 内平方并归约到 8 行，再做最终标量收敛。其余路径保持 S03J 行为。

## 3. 目标 A/B/A 结果

基线为独立安装的 S03J，候选为独立安装的 S03K。每点取 5 次 NPU Event
中位数，顺序为 S03J-1 / S03K / S03J-2。

| 形状类别 | dtype | S03J 中位基线（μs） | S03K（μs） | 提升 |
| --- | --- | ---: | ---: | ---: |
| `[64,8192,2]` | fp16 | 815.160 | 80.430 | 90.13% |
| `[64,8192,2]` | bf16 | 825.520 | 81.660 | 90.11% |
| `[64,8192,2]` | fp32 | 753.155 | 79.650 | 89.42% |
| `[64,8192,4]` | fp16 | 760.690 | 82.550 | 89.15% |
| `[64,8192,4]` | bf16 | 769.050 | 79.390 | 89.68% |
| `[64,8192,4]` | fp32 | 667.085 | 82.300 | 87.66% |
| `[64,8192,8]` | fp16 | 669.535 | 80.630 | 87.96% |
| `[64,8192,8]` | bf16 | 671.195 | 85.200 | 87.31% |
| `[64,8192,8]` | fp32 | 408.585 | 81.780 | 79.98% |
| `reduce=2048,inner=8` | fp16 | 219.275 | 81.910 | 62.65% |
| `reduce=2048,inner=8` | bf16 | 221.090 | 85.270 | 61.43% |
| `reduce=2048,inner=8` | fp32 | 170.175 | 83.590 | 50.88% |
| 奇数 `reduce=4097,inner=8` | fp16 | 413.875 | 79.500 | 80.79% |
| 奇数 `reduce=4097,inner=8` | bf16 | 420.415 | 83.830 | 80.06% |
| 奇数 `reduce=4097,inner=8` | fp32 | 292.335 | 81.440 | 72.14% |

聚合提升为 84.7826%，15 个点均超过预设的 15% 单点门槛。

## 4. 控制组与正确性

控制组覆盖 `inner=9`、输出行数 39、`reduce=2047`、fastPath3 和 fastPath4，
三种 dtype 共 15 点。全部正确，最大表观回退 2.0023%，最大基线漂移
2.7833%，没有观察到阈值外结构性退化。

正确性套件：

| 套件 | 通过数 |
| --- | ---: |
| directed | 46 |
| BF16 语义 | 4 |
| random | 150 |
| extended | 726 |
| S03J 边界 | 135 |
| S03K 新路径边界 | 69 |
| 合计 | **1130/1130** |

S03K 边界覆盖 inner 2～9、行数 39/40/41、reduce
2047/2048/2049、奇数长尾、负轴、乱序轴、keep_dims、高维等价布局和
singleton gap。

## 5. 构建、包与可追溯性

第一次试构建虽然成功，但 `op_kernel/CMakeLists.txt` 尚未将新增 key 6
加入 `--tiling_key`，运行时报告入口不存在；补齐注册后重新从源码构建，
上述所有有效性能和正确性结果均来自修复后的独立安装。

```text
Host SHA-256:
4abd44e6f30acb14d9040590f1a5ebe2a21b7edd43ce167da123fe913426e91a

Kernel SHA-256:
b98a1773e94995027ae488194c1382285b8a1da8d56cba92ca09cafa4c8c28b9

Kernel CMake SHA-256:
775e52c863e985de329cfd520007afe1ccad1124c1728dff87a4203440d732ff

RUN SHA-256:
31c302e91265848621ef502c2508b348b8591ef75bf9146ad9fa99570a24a47d

ZIP SHA-256:
9850faf54274057338b76a6ea3a125c5d03fddc2dffd2c9e2769f6e864ed0365
```

提交文件：

```text
D:\29722\Desktop\GCC\提交相关材料\20260807\S03K\SquareSumV1.zip
```

ZIP 已检查为 9 个条目、单一顶层目录 `SquareSumV1_zip/`，包含五个源码/
CMake 文件和经过验证的 `.run`，其中 `.run` 权限为 0755。
