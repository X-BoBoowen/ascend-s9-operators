# SquareSumV1-S02CE 云端验证与待测评交接

更新时间：2026-08-06（Asia/Shanghai）

## 1. 结论

S02CE 是 S02CD 的通用并行度修正：fastPath3 的输出数为 9～32，且输入
规模、归约规模、连续尾段和归约行数达到既有 split-K 安全门限时，使用
40 核 workspace split-K，不再把 32 个输出按宽度 8 分为 4 个任务而只
启动 4 核。

该候选在 Ascend 910B4、CANN 社区版 8.5.0 上通过：

```text
新增输出 9～32 边界及回退检查  42/42
既有 S02BT/S02CA/S02CB 回归      147/147
42 点全路径图谱                  42/42
```

正式平台结果尚未返回，因此不能宣称达到
`0.30 × T_baseline = 1195.599 us`。

## 2. 根因与修改

公开代表布局 `(64, 4, 8, 2048), axes=(0, 3)` 有 32 个输出。S02CD 会把
输出按 8 个一组，形成 4 个任务，因此只启动 4 个 AICore；每个核顺序
扫描 64 个批次归约行。S02CE 将 `NONCONTIGUOUS_SPLITK_MAX_OUTPUTS` 从
8 扩大到 32，使满足全部通用安全条件的布局按归约行分配到最多 40 核，
每核写 FP32 partial，最后统一归并。

路由仍只依赖输入/输出规模、归约规模、连续尾长、归约行数、shape、axis
和物理 stride。没有针对官方隐藏样例硬编码。

## 3. 性能证据

同一公开代表布局的独立 9 组采样 A/B：

```text
FP16: 150.160 -> 55.716 us（2.695x）
FP32: 152.547 -> 54.340 us（2.807x）
```

完整 42 点图谱由于运行时波动，该点复验为 FP16 `66.120 us`、FP32
`64.727 us`，仍显著优于 S02CD。图谱总体为：

| 版本 | 42 点中位总和（us） | 最大单点（us） |
| --- | ---: | ---: |
| S02CD | 3459.513 | 152.547 |
| S02CE | 2985.773 | 124.940 |

S02CE 总和下降 `13.70%`，且没有图谱点退化超过 15%。

## 4. 正确性覆盖

新增 14 个布局、3 种 dtype，共 42 项，覆盖：

- 输出数 9、15、16、17、18、21、24、31、32；
- 输出数 33 的门限外回退；
- 连续尾长 1024 和 16384 的边界，以及 1023、16385 的回退；
- 归约行数 16、17、32，以及 15 的回退；
- rank3/rank4、多输出维、3 个归约轴、负轴、乱序轴和 keep_dims；
- FP16、BF16、FP32。

此外重新执行既有 147 项边界，全部通过。

## 5. 构建与提交包

云端实验：

```text
/home/ma-user/work/s9/experiments/squaresum_s02ce_splitk_o32_20260806_1600
```

云端发布快照：

```text
/home/ma-user/work/s9/releases/squaresum_s02ce_20260806_1610
```

本地提交包：

```text
D:\29722\Desktop\GCC\提交相关材料\20260806\S02CE\SquareSumV1.zip
大小：829605 bytes
ZIP SHA-256：BBE62860C2DB8891F5A6699FEFF649C46CEBF9D935BD23D2C81633B4C3EB1D70
RUN SHA-256：FE7DAAB7DA5D4162ABA12F613B412D6BF8D263827EB95316D769889BA9C66B35
```

ZIP 只有一个顶层目录 `SquareSumV1_zip/`，共 6 个文件；`.run` 权限为
`0755`。

## 6. 官方结果上下文

```text
S02BA baseline: 6.510 / 399.088 / 138.753 / 2550.761 / 890.218
prof_sum = 3985.330 us

S02CA: 6.410 / 390.238 / 138.633 / 2557.111 / 890.177
prof_sum = 3982.569 us

S02CD: 6.390 / 397.4975 / 139.603 / 2556.431 / 894.4275
prof_sum = 3994.349 us
```

S02CA 与 S02CD 都属于无效波动；尤其 S02CD 证明公开 fastPath4 图谱改善
没有命中正式负载。S02CE 改动的是另一条 fastPath3 路由，仍只有在官方
五项全部 Pass 且正式结果显著下降后，才能认定为有效候选。
