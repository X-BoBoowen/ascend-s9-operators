# S9 专家评审跟踪表

来源：

- `D:/29722/Desktop/GCC/check.md`
- `D:/29722/Desktop/GCC/fable/docs/S9_五题严格代码评审与优化方案.docx`
- `D:/29722/Desktop/GCC/fable/docs/S9_算子优化策略详解.docx`
- 上述文档点名的 10 个往届仓库快照

详细取舍见：`EXPERT_OPINION_SYNTHESIS_20260727.md`

使用原则：评审意见是审计线索，不是事实来源。每项必须与 S9 原始
Excel、官方脚本、当前源码、NPU 正确性对拍和 profiler 数据交叉验证。

## 全局验收纪律

- [x] 不以公开 Case1 通过作为完成标准。
- [x] 每题使用正式 `.run` 安装后的自定义 `aclnn` 接口验证。
- [x] 正确性覆盖全部题面 dtype、负轴/多轴/广播、非对齐和大张量。
- [x] 整型与 bool 使用逐位相等；浮点按题面阈值并补专项边界测试。
- [ ] 五题最终源码、构建产物、独立安装、SHA-256 和材料包一致性复核。
- [ ] 平台 Case1–Case5 全部通过后记录榜单性能。

## Concat

- [x] 当前实现按真实 dim 折叠 outer/inner，支持负轴和 rank 1–8。
- [x] 支持 float32/float16/int32/int8、动态输入数和非对齐尾块。
- [x] 9 个定向用例 + 100 个随机用例逐位相等。
- [x] 16 核为当前实测最优；8/32 核和指针缓存方案均已回退。
- [x] 最终独立重建和材料包一致性复核；提交包已更新。

## Greater

- [x] 支持题面五种 dtype、rank 1–8 和完整广播。
- [x] NaN/Inf/±0 专项与随机对拍通过。
- [x] 同形、标量广播、连续广播和通用步长映射均有对应路径。
- [x] int32 保留精确标量比较；已否决会破坏边界正确性的向量近似。
- [x] 最终 int8 packed-scalar 版本已重建并重新计算 SHA-256。
- [x] 最终独立重建和材料包一致性复核；提交包已更新。

## IndexAdd

- [x] 输出直接从 self 读入并累加，不依赖 wrapper 预置。
- [x] 支持五种 dtype、rank 1–8、任意合法 dim/负轴。
- [x] 按输出目标分组，重复 index 保持参考顺序，无跨核写冲突。
- [x] 空 index 返回 self，非对齐使用 DataCopyPad。
- [x] Excel 复核：M∈[1,8000]；当前缓存容量 8000，专家将 N 上限误作 M。
- [x] M=8000 题面上限边界对拍通过。
- [x] 最终版完成 23 定向 + 170 原随机 + 345 扩展矩阵，共 538 项通过。
- [x] 使用编译期 TilingKey：普通路径选择稳定版；仅高碰撞 int8
  （`indexCount > dimSize * 4`）选择两次加法后回绕路径。
- [x] 910B4 task-time：公开形状约 9.108µs；高碰撞 int8 约
  256.659µs，优于稳定路径约 332.607µs。
- [x] bf16 保持每次加法后转回 bf16，未采用会改变逐步舍入的 FP32
  全程累加；int8 使用精确模 256 回绕。
- [x] 最终源码、`.run` 与 ZIP 逐文件 SHA-256 一致；ZIP 已回传云端
  解压复核，结果 `FINAL_INDEXADD_PACKAGE_REMOTE_VERIFY_OK`。
- [x] `提交相关材料/IndexAdd.zip` 已更新；旧包保存在
  `提交相关材料/旧包备份_20260727-112236/IndexAdd.zip`。

## Transpose

- [x] 支持四种 dtype、rank 1–6、任意合法排列和非对齐。
- [x] 恒等复制、FP16 16×16 单矩阵/批矩阵、连续输出 run、循环后缀
  二维化、跨步 Gather 和通用回退均已实现。
- [x] 48 个定向 + 200 个随机 + 152 个扩展用例，共 400 项逐位相等；
  扩展集包含负轴、tile 边界、1–6 维、四种 dtype、NaN/Inf/负零
  位模式及非循环排列。
- [x] 循环置换 `(32,64,128)`：task-time 约 10.818/9.879µs；
  原路径同规模接近 1ms。
- [x] 任意 rank-4 示例从约 1024.657µs 降至约 134.399µs；
  NCHW→NHWC/NHWC→NCHW（1,048,576 元素）约 16.357/16.307µs。
- [x] 公开 FP16 128×256 路径约 4.44–4.60µs；40 核实测
  约 5.130µs，已否决并恢复 32 核。
- [x] 最终源码、`.run` 与 ZIP 哈希固化；根目录、文件清单和 Unix
  可执行权限已在本地与云端复核，结果
  `FINAL_TRANSPOSE_PACKAGE_REMOTE_VERIFY_OK`。
- [x] `提交相关材料/Transpose.zip` 已更新，SHA-256 为
  `0b88b825e7ee6e5cb598e5c3eb4638d4f652a4a0eb32671087a10a4aa3ff57e8`；
  旧包保存在 `提交相关材料/旧包备份_20260727-1240/Transpose.zip`。

## SquareSumV1

- [x] 支持 float16/bfloat16/float32、负轴、多轴和 keep_dims。
- [x] host 推形与 kernel 轴语义一致，使用 FP32 中间计算。
- [x] 46 个定向用例 + 150 个随机用例通过。
- [x] 后缀轴、中间轴和分离轴已有向量路径。
- [ ] 继续优化剩余通用归约模式并补极值/长归约专项。
- [ ] 最终独立重建和材料包一致性复核。

## 已明确否决或需谨慎的专家建议

- “IndexAdd 的 index 可能到 10000”：原始 Excel 明确 M≤8000，错误。
- “fp16/bf16 IndexAdd 统一 FP32 累加”：可能改变逐次加法舍入顺序，
  不能直接采用；必须与 `torch.index_add` 的对应 dtype 语义对拍。
- “重复 index 使用原子加”：不是唯一正解。当前按目标输出分组，
  每个输出位置仅由一个任务写入，既避免竞态也保留确定性顺序。
- “所有算子统一双缓冲”：只有 profiler 证明搬运可隐藏且 UB 预算允许
  时才采用，不能把双缓冲当作自动提速规则。
- “往届优秀代码可以直接复用”：10 个点名仓库中存在 310B 专用参数、
  固定核数/UB/rank、标量通用路径、错误推形、未完成分支和潜在越界；
  只提取经 CANN 8.5、S9 题意和 910B4 实测验证的局部模式。
