# SquareSumV1-S02CL 云端验证与待测评交接

更新时间：2026-08-06（Asia/Shanghai）

## 1. 结论

S02CF 的正式结果与 S02CD 完全一致：

```text
6.390 / 397.4975 / 139.603 / 2556.431 / 894.4275
prof_sum = 3994.349 us
```

因此，S02CF 的 fastPath4 窄 inner 优化没有命中官方主要耗时路径，不能
视为有效正式优化。S02CL 转向 fastPath4 的 `inner > 16` 大输入调度，只
修改 Host 侧分核数量，不修改 kernel、数据格式或算子语义。

Ascend 910B4、CANN 社区版 8.5.0 最终验证：

```text
新增边界与 dtype 正确性           45/45
累计七组正确性回归              315/315
32M 大规模全 fastPath 图谱        26/26
4M 小规模全 fastPath 图谱         42/42
云端发布包审计                     PASS
```

S02CL 尚无正式平台结果。公开真机探针只能证明改动在相应通用布局上有效，
不能证明已经命中隐藏 Case，也不能据此宣称达到 `1195.599 us` 目标。

## 2. 根因与实现

原 fastPath4 在 `inner > 16` 时按固定“每核目标输出数”分核。对于输出由
16～40 个完整 inner 行组成的大输入，这会让一个核跨越完整行边界工作，
既减少并行度，也增加尾部处理。

S02CL 在以下通用条件下把一个完整输出行交给一个核：

- `fastPath == 4` 且 `inner > 16`；
- 输入达到既有大输入门限；
- 输出可拆为 16～40 个完整 inner 行；
- FP16/BF16，或 FP32 inner 已按 32 字节 block 对齐。

FP32 非对齐 inner 仍使用 40 核；输出行少于 16 或大于 40、`inner <= 16`
以及小输入全部保持旧调度。条件只依赖公开 shape、dtype、对齐和硬件核数，
不包含 Case 编号或隐藏数据特征。

## 3. 性能证据

相同输入的独立 A/B 目标点：

| 布局 | dtype | S02CG（us） | S02CL（us） | 变化 |
| --- | --- | ---: | ---: | ---: |
| 31 行，inner=65 | FP16 | 422.56 | 333.40 | -21.10% |
| 31 行，inner=65 | FP32 | 383.85 | 377.36 | -1.69% |
| 16 行，inner=256 | FP16 | 123.54 | 110.80 | -10.31% |
| 16 行，inner=256 | FP32 | 158.80 | 109.93 | -30.77% |
| 24 行，inner=256 | FP16 | 121.11 | 88.92 | -26.58% |
| 24 行，inner=256 | FP32 | 135.47 | 90.69 | -33.05% |
| 40 行，inner=33 | FP16 | 883.98 | 560.25 | -36.62% |
| 40 行，inner=33 | FP32 | 788.36 | 556.06 | -29.47% |

rank5、32 行、inner=128 的独立点：

```text
FP16: 149.85 -> 81.01 us（下降 45.94%）
FP32: 158.94 -> 78.73 us（下降 50.46%）
```

完整 26 点大规模图谱总和为 `6068.880 -> 5991.740 us`（下降 1.27%）。
该总和包含大量没有修改的路径，且个别点有运行噪声；应以直接目标 A/B
解释改动效果。42 点小规模图谱为 `3047.187 -> 3065.800 us`（+0.61%），
属于几十微秒启动噪声范围，没有结构性退化。

## 4. 正确性覆盖

新增 45 项覆盖 FP16、BF16、FP32，以及：

- 输出行边界 15、16、40、41；
- inner 17、33、65、128、129、256；
- FP32 对齐与非对齐；
- rank5、keep_dims、负轴和无序轴；
- 低于大输入门限及 `inner=16` 的回退路径。

随后重跑此前六组用例与本轮新增组，累计 `315/315`；大规模图谱
`26/26`、小规模图谱 `42/42` 全部通过。

## 5. 路径、提交包与哈希

云端实验：

```text
/home/ma-user/work/s9/experiments/squaresum_s02cl_fast4_aligned_rows_min16_20260806_1720
```

关键日志：

```text
correctness_s02cl.log
cumulative_correctness_315.log
large_atlas_s02cl.log
path_atlas_s02cl.log
```

云端发布快照：

```text
/home/ma-user/work/s9/releases/squaresum_s02cl_20260806_1732
```

本地提交包：

```text
D:\29722\Desktop\GCC\提交相关材料\20260806\S02CL\SquareSumV1.zip
大小：865936 bytes
ZIP SHA-256：A4C82815EF2FAF8E96FC6D310EAC0EE4CC17D41CD0ECE3FF1DEE38F23891044B
RUN SHA-256：459E63207CADD4226537F0168D318855BC41B55AA8E14C23EF8CAC9BA7643F06
```

ZIP 仅有顶层 `SquareSumV1_zip/`，共 9 个目录/文件条目。源码权限为
`0640`，`.run` 为 `0755`。本地 ZIP、云端发布 ZIP、5 份正式源码和
经过 315 项回归的构建产物均已逐字节核对一致。

## 6. 正式结果边界

```text
S02BA baseline = 3985.330 us
S02CD           = 3994.349 us
S02CF           = 3994.349 us（相对 S02CD 零变化，淘汰）
S02CG           = 尚待正式结果
S02CL           = 尚待正式结果
目标            = 1195.599 us
```

正式评测应上传 S02CL 包，并以五个 Case 全部 Pass 后的 `prof_sum` 判断
是否晋级。若官方结果仍无变化，应立即停止继续微调该调度分支，转而通过
官方 Case 耗时差分寻找尚未覆盖的算法路径。
