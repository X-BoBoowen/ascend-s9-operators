# SquareSumV1 S03O 云端正确性、性能与打包门禁

日期：2026-08-07（Asia/Shanghai）

## 1. 结论

S03O 已通过内部提交门禁并生成正式评测包，但尚无比赛平台五 Case 成绩：

- 最终安装包完整正确性：**1325/1325**；
- FP16/BF16 目标 A/B/A 聚合改善：**50.9727%**；
- 最差目标单点改善：**50.5274%**；
- 两轮目标基线最大漂移：**0.2887%**；
- 五个关键回退控制点最大表观回退：**0.3094%**；
- ZIP 结构、源码逐字节一致性和 `.run` 0755 权限审计通过。

以上是 Ascend 910B、CANN 8.5.0 上的内部正式兼容 profiling 结果，
不是比赛平台隐藏 Case 成绩。只有平台五 Case 返回值能够决定 S03O 是否
替代正式最好 S02F（`3223.995 us`）。

## 2. 实现与选择性路由

S03O 是 S02F 之后三阶段、逐阶段验证的 fastPath4 优化：

1. S03M / key 7：对 32 字节对齐的 rank-5 非连续归约，把物理相邻的
   2/4/8 个保留行合并为一次连续 DMA，再在 UB 内分别平方和归约；
2. S03N / key 7：把最内归约维从仅 2 次幂扩展到任意合法长度，奇数尾部
   仍由通用归约处理；
3. S03O / key 8：为非 8 对齐的小 inner 增加连续行 DMA、32 字节 UB
   行步长和紧凑二维写回。

S03O 初始通用 key 8 在 `inner=15/17` 上有收益，但在 `inner=31/63` 和
奇数归约 127 上明显回退。最终路由因此只在以下通用硬件成本条件同时成立
时启用 key 8：

- 输入不少于 1 Mi 元素、归约不少于 2048、独立任务不少于 32；
- `fastPath == 4`，最内归约维大于 1 且为 2 次幂；
- `inner` 非 8 对齐且不超过 16；
- dtype 元素宽度为 2 字节，即 FP16 或 BF16；
- UB 容量、行对齐、输出容量和 DMA 参数均满足安全边界。

FP32、奇数归约、`inner > 16`、容量不足和其他 fastPath 均回退到已验证的
S03N/S03M 或原 S02F 路径。路由不使用平台 Case 编号、隐藏 shape 或固定
隐藏数据。

## 3. 完整正确性

最终 `.run` 独立安装后从头运行全部套件：

| 正确性套件 | 通过数 |
| --- | ---: |
| directed | 46 |
| BF16 语义 | 4 |
| random | 150 |
| extended | 726 |
| S03J 边界 | 135 |
| S03K 边界 | 69 |
| S03M 分组行边界 | 105 |
| S03N 任意归约边界 | 45 |
| S03O 非对齐 inner 边界 | 45 |
| 合计 | **1325/1325** |

S03O 专项覆盖 FP16、BF16、FP32，`inner=1/2/7/8/9/15/16/17/31/33/
63/65/127/129/199`，负轴、乱序轴、`keep_dims`、奇数归约、对齐路径和
容量回退。最终日志明确返回 `FINAL_CORRECTNESS_PASS total=1325`。

## 4. 目标 A/B/A

基线为独立安装的 S03N，候选为最终 S03O 安装包。每次 msprof 运行要求
30 个 SquareSumV1 非乘法任务，跳过 30 个乘法任务，并按正式兼容规则
选择计分区间。两轮基线漂移必须不超过 3%，总体和每个启用目标点的改善
均必须不少于 50%。

| dtype | S03N 中位基线（us） | S03O（us） | 改善 | 基线漂移 |
| --- | ---: | ---: | ---: | ---: |
| FP16 | 2587.8813 | 1257.1950 | 51.4199% | 0.0217% |
| BF16 | 2598.5618 | 1285.5760 | 50.5274% | 0.2887% |

两点基线总和 `5186.4430 us`，候选总和 `2542.7710 us`，聚合改善
`50.9727%`。完整原始值见
`candidates/squaresum_s03o_unaligned_grouped_rows_20260807/evidence/
target_comparison.json`。

## 5. 回退控制

五个控制点复测了所有曾发生结构性回退的区域。它们均由最终 Host 路由
回退到 S03N：

| 控制点 | dtype | S03N（us） | S03O（us） | 表观回退 |
| --- | --- | ---: | ---: | ---: |
| reduce128 / inner15 | FP32 | 2003.2100 | 2003.5300 | 0.0160% |
| reduce127 / inner15 | FP16 | 2559.2510 | 2564.3710 | 0.2001% |
| reduce128 / inner17 | FP16 | 2481.8300 | 2489.0990 | 0.2929% |
| reduce128 / inner31 | FP16 | 1030.8510 | 1034.0400 | 0.3094% |
| reduce128 / inner63 | FP16 | 534.6110 | 533.5405 | -0.2002% |

最大表观回退 `0.3094%`，低于 3% 门槛。控制基线来自同一实例、同一
S03N 安装包的既有正式兼容采样；目标域则使用本轮两次独立基线完成严格
A/B/A。控制明细见同一 evidence 目录的 `control_comparison.json`。

## 6. 构建与提交包

```text
Git main commit:
4b8657c

Host SHA-256:
4da1c386938fe028d3955bf4d1ba262a8fba31479bbb8b7c59f49a678478c800

Tiling header SHA-256:
e669820ab05878a8701fe2e6f33f4b44076ee93e74c1a308c3b5e8fcf54079f6

Kernel SHA-256:
1bb56c08c6c7d11aaf224035090eb21316da4a960f699ccdc8ef0ad7f4a187c7

RUN SHA-256:
4adeaeea2f5524685835079c6a17a541b7dda9da5339f405931e1ce77342b3f1

ZIP SHA-256:
67aeae465123c0cafadcb8da2870fe707dfe1e2b96c4fdebb9885201cbe972d3
```

提交文件：

```text
D:\29722\Desktop\GCC\提交相关材料\20260807\S03O\SquareSumV1.zip
```

ZIP 大小 `495610` 字节，含 9 个条目、唯一顶层目录
`SquareSumV1_zip/`。五个源码/CMake 文件和已验证 `.run` 均逐字节一致，
`.run` 在 ZIP 中权限为 0755。

## 7. 决策边界

S03O 是当前唯一推荐送交正式平台的候选，但内部局部目标改善不能证明隐藏
五 Case 一定命中新路径，也不能证明已达到 `0.30 x T_baseline`。平台结果
返回前不得宣称替代 S02F、进入前十或完成最终目标。正式结果应逐 Case 与
S02F 对照；若主要耗时项没有下降，应保留本轮证据并继续做通用路径归因。
