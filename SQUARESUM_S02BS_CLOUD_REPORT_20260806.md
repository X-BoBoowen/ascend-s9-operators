# SquareSumV1-S02BS 云端验证与待测评交接

更新时间：2026-08-06（Asia/Shanghai）

## 1. 结论

S02BS 是 S02BN 的后继候选，并保留 S02BP 的窄 inner 扩核调度。它修复
fastPath4 中 `inner=1` 的主要指令瓶颈：旧实现按“一个输出 × 一个末级
归约行”发起小块 DMA，并为每个标量行补齐到 32 字节；S02BS 将物理连续
的 8 个输出行合并为一个任务，一次搬运 8 段完整末级归约行，再用硬件
整行归约指令同时生成 8 个 partial。

同机配对热点中，三种 dtype 合计由 `6615.292 us` 降至 `140.604 us`，
加速 `47.049x`。规模交叉的 12 个配对点全部加速，合计由
`5681.952 us` 降至 `371.264 us`，加速 `15.304x`；最小单项加速
`1.090x`，最大 `46.067x`。候选在 Ascend 910B4、CANN 社区版 8.5.0
上原生构建并通过累计 `1658/1658` 正确性门禁。

正式平台五个 Case 尚未返回，因此不宣称达到排行榜目标。正式官方基线
仍为 S02BA 的 `3985.330 us`，目标为 `1195.599 us`。

## 2. 通用路由

新路由同时满足：

```text
fastPath == 4
reduceMode == 0
innerElements == 1
末级归约维物理步长 == 1
末级归约长度为 2、4、8、16、32 或 64
存在物理步长等于末级归约长度的保留输出维，且该维长度 >= 8
8 行片上缓冲不超过 8192 个输入元素
按 8 行分组后至少形成 32 个任务
```

满足时复用构建系统已经打包的 tiling key 5。Kernel 仅在
`fastPath == 4` 时进入 `ProcessStridedGroupedRows`；原 key 5 的
fastPath3 grouped-suffix 行为保持不变。每个任务严格限制在同一个保留维
内部，不跨越外层输出边界；非 8 整除尾部通过 `activeRows` 处理。

这些条件只依赖 shape、axis、dtype 和物理步长，不依赖 Case 编号、输入
值或隐藏数据。源码：

```text
candidates/squaresum_s02bs_strided_grouped_scalar_rows_20260806/SquareSumV1
```

实现提交：`b2c954a`；边界与规模测试提交：`ae56661`、`ca9903d`。

## 3. 性能证据

### 3.1 1M 元素热点

布局为通用的交错归约：shape `(64, 256, 64, 1)`、axis `(0, 2)`。

| dtype | S02BP（us） | S02BS（us） | 加速 |
| --- | ---: | ---: | ---: |
| FP16 | 约 2202 | 46.632 | 约 47x |
| BF16 | 约 2200 | 47.468 | 约 46x |
| FP32 | 约 2210 | 46.504 | 约 47x |
| 合计 | 6615.292 | 140.604 | 47.049x |

路径矩阵全部 33 项合计由 `17975.302 us` 降至 `11680.390 us`，加速
`1.539x`。未进入新路由的 inner2/4/8/16/32、inner33/64、单输出和
rank5 控制项保持既有实现；其差异主要处于复测波动范围。

### 3.2 规模交叉

| 布局 | dtype | S02BP（us） | S02BS（us） | 加速 |
| --- | --- | ---: | ---: | ---: |
| 512 输入、末级 2 | FP16 | 28.159 | 25.835 | 1.090x |
| 512 输入、末级 2 | FP32 | 28.363 | 25.605 | 1.108x |
| 6144 输入、末级 8 | FP32 | 29.613 | 25.381 | 1.167x |
| 131072 输入、末级 32 | FP16 | 265.603 | 30.977 | 8.574x |
| 131072 输入、末级 32 | FP32 | 273.456 | 29.993 | 9.117x |
| 1048576 输入、末级 64 | FP16 | 2202.356 | 47.808 | 46.067x |
| 1048576 输入、末级 64 | FP32 | 2307.832 | 55.988 | 41.220x |
| 257 行尾部 | FP32 | 122.539 | 25.848 | 4.741x |
| 多外层输出行 | FP32 | 137.764 | 26.052 | 5.288x |

12 个配对点全部为正收益，因此无需增加输入规模阈值。

## 4. 正确性门禁

```text
主矩阵：定向、cloud gate、随机、BF16、扩展       971/971
既有专项：middle8、单例、split-K、workspace 等    618/618
S02BS 新路径边界                                  36/36
S02BS 路径 A/B 正确性                             33/33
累计                                             1658/1658
```

新边界覆盖末级归约 2/4/8/16/32/64、257 行非整除尾部、多个外层输出
行、`keep_dims=False/True`、单例间隔、负轴和轴重排，三种 dtype 全过。

## 5. 失败中间版本

- S02BQ 使用新 tiling key 14；CANN 包只编译 key 1～13，运行时报
  `BinaryGetFunctionByEntry failed, funcEntry=14`，已废弃。
- S02BR 复用 key 5 后成功进入新路径，但紧凑树形加法末级地址未按
  32 字节对齐，设备报告 `UB address accessed by VEC is not aligned`，
  已废弃。
- S02BS 将该场景收紧为 `inner=1、末级归约<=64`，改用对齐的
  `WholeReduceSum`，上述两个问题均消失。

失败版本不作为提交包，也没有替换 S02BS 的最终云端安装状态。

## 6. 构建、安装与提交包

云端实验：

```text
/home/ma-user/work/s9/experiments/squaresum_s02bs_cloud_20260806_1324
```

云端发布快照：

```text
/home/ma-user/work/s9/releases/squaresum_s02bs_20260806_1341
```

实际安装并完成门禁的 RUN SHA-256：

```text
5DABA1D2AF285683D8BD755A549EB112145126EDFE04A815A18A461B0C5FD409
```

提交包：

```text
D:\29722\Desktop\GCC\提交相关材料\20260806\S02BS\SquareSumV1.zip
大小：687510 bytes
SHA-256：65CA55C54F3FE3A388411A109C024A7101EA0BC66E40BE4959179AF5396409FE
```

ZIP 只有一个顶层 `SquareSumV1_zip/`，包含五份 host/kernel 源文件和
一个 `.run`，共 6 个文件；ZIP 内 `.run` 权限为 `0755`。本地 ZIP 与
云端发布 ZIP 哈希一致，发布源码与候选源码逐文件一致。

## 7. 结论边界与下一步

S02BS 已满足本地与云端交付门槛，但公开矩阵不能证明隐藏 Case 命中该
布局。下一步上传本报告指定的 ZIP，记录官方 Case1–Case5；只有五项全
Pass 且官方 `prof_sum` 稳定下降，才将 S02BS 晋级为新的正式基线。
