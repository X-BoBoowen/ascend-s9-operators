# SquareSumV1-S02BB 云端验证与阶段测评交接

更新时间：2026-08-05 17:12（Asia/Shanghai）

## 1. 官方基线与目标

S02BA 官方平台结果：

```text
Case1: Pass, Result: 6.51
Case2: Pass, Result: 399.088
Case3: Pass, Result: 138.753
Case4: Pass, Result: 2550.761
Case5: Pass, Result: 890.218
prof_sum: 3985.33
```

本阶段将 `T_baseline` 固定为 `3985.33 μs`，目标为：

```text
0.30 × T_baseline = 1195.599 μs
```

该目标比已知第 10 名参考线 `1268.598 μs` 还低 `72.999 μs`。
S02BA 的 Case4 占总耗时 `64.0%`，但隐藏 shape、dtype、轴和执行路径
不可见，不能从单个 Case 编号反推或硬编码。

与历史 S02E2 官方结果逐项比较：

| Case | S02E2（μs） | S02BA（μs） | 差值 | 结论 |
| --- | ---: | ---: | ---: | --- |
| 1 | 6.3505 | 6.510 | +0.1595 | 变慢 2.51% |
| 2 | 400.158 | 399.088 | -1.070 | 改善 0.27% |
| 3 | 239.505 | 138.753 | -100.752 | 改善 42.07% |
| 4 | 1718.344 | 2550.761 | +832.417 | 变慢 48.44% |
| 5 | 895.168 | 890.218 | -4.950 | 改善 0.55% |
| 合计 | 3259.5255 | 3985.330 | +725.8045 | 变慢 22.27% |

这张表只证明两个完整版本的端到端差异，不证明某个隐藏 Case 使用了
哪条代码路径。S02BA 不晋级为正式基线，保留作诊断和回退证据。

## 2. S02BB 优化内容

公开矩阵发现 workspace 中间归约的一个结构性慢路：完整内层宽度为 8
时，FP16/BF16 每个物理连续行只有 16B。旧 Kernel 使用多块
`DataCopyPad`，将每一行分别搬入并补到 32B，随后才执行平方、转 FP32
和树归约。超长归约会产生大量 16B 短块搬运。

S02BB 仅在以下条件全部满足时启用紧凑搬运：

- 已由 Host 选择 workspace 中间连续归约；
- 当前处理完整内层，`innerElements == current == 8`；
- `innerIndex == 0`；
- dtype 为 FP16 或 BF16。

命中时，将同一归约片段的 `rows × 8` 元素作为一个物理连续块搬入 UB，
不再逐行补齐；后续平方、BF16 舍入语义、FP32 累加、40 核 workspace
归约和最终回写保持不变。FP32、宽度 4/16、非 workspace 路径以及全部
Host Tiling 不变。

候选源码：

```text
candidates/squaresum_s02bb_compact_middle8_20260805/SquareSumV1
```

相关提交：

```text
c6f2d7b perf(squaresum): compact half middle width eight
eefae6b test(squaresum): cover compact middle width eight
```

## 3. 同形状 A/B

测量环境为同一台 Ascend 910B4、CANN 社区版 8.5.0；分别安装 S02BA
和 S02BB，预热后用 NPU Event 取 7 组样本中位数。每项同时与 CPU
参考结果比较。

| 形状 / 轴 / dtype | S02BA（μs） | S02BB（μs） | 加速 |
| --- | ---: | ---: | ---: |
| `(128,1,256,8)` / `(0,2)` / FP16 | 59.511 | 32.263 | 1.84× |
| 同上 / BF16 | 61.041 | 31.083 | 1.96× |
| `(100,1,1000,8)` / `(0,2)` / FP16 | 143.534 | 33.393 | 4.30× |
| 同上 / BF16 | 140.803 | 39.546 | 3.56× |
| `(200,1,1000,8)` / `(0,2)` / FP16 | 273.120 | 37.602 | 7.26× |
| 同上 / BF16 | 273.421 | 36.392 | 7.51× |
| `(1000,1,1000,8)` / `(0,2)` / FP16 | 1270.352 | 51.440 | 24.69× |
| 同上 / BF16 | 1245.852 | 51.456 | 24.21× |
| `(2,200,1,1000,8)` / `(1,3)` / FP16 | 525.636 | 39.904 | 13.17× |
| 同上 / BF16 | 532.690 | 39.094 | 13.63× |

控制路径：

- 同形状 FP32 中位数基本不变，例如 `136.112 → 136.370 μs`；
- 宽度 4、16 的三种 dtype 保持旧路径，差异处于独立运行波动范围；
- `reduceElements=8192` 时输入总量未达到 workspace 门槛，仍走旧路径，
  FP16 `123.775 → 123.801 μs`；
- 去掉单例间隔的 `(200000,8)` 同样从约 `272 μs` 降至约 `35 μs`，
  证明优化针对通用物理布局，不依赖 S02BA 的单例轴形式。

## 4. 正确性与回归证据

S02BB 安装后通过：

```text
定向矩阵：46/46
随机矩阵：150/150
BF16 严格语义：4/4
扩展边界矩阵：726/726
关键路径正确性门：45/45
宽度 8 紧凑搬运专项：60/60
单例间隔语义专项：72/72
单例中间交叉矩阵：30/30
单例间隔性能矩阵：15/15
宽度 8 性能矩阵：27/27
```

60 项专项覆盖 workspace 输入门槛前、门槛点和门槛后，奇数归约长度，
单例间隔，多个外层输出组，正轴、负轴、乱序轴，`keep_dims` 真/假，
FP16、BF16、FP32，以及宽度 7/9/16 控制路径。

曾尝试把紧凑布局直接推广到宽度 1/2/4（S02BC），首个宽度 1 用例即
触发 AIVector UB 地址未按 32B 对齐。该候选已否决并恢复 S02BB；恢复后
60/60 专项再次通过，已安装 AICore 对象逐文件与 S02BB 构建产物一致。
失败证据保留，但 S02BC 不进入本包。

## 5. 构建产物与提交包

云端实验：

```text
/home/ma-user/work/s9/experiments/squaresum_s02bb_cloud_20260805_1654
```

云端 `.run`：

```text
大小：594347 bytes
SHA-256：50AAF086075564114EA2B3AB015ECC51F17AE6A13DE120C590FCD4CDF0D1A072
```

官方阶段测评 ZIP：

```text
D:\29722\Desktop\GCC\提交相关材料\20260805\S02BB\SquareSumV1.zip
大小：591340 bytes
SHA-256：DC7451016F341784E3083653DD9BCCBF32B0345A3F9FAA462E76162682289F7F
```

ZIP 根目录为 `SquareSumV1_zip/`，只含 `op_host/`、`op_kernel/` 和
`custom_opp_euleros_aarch64.run`。五份源码与 S02BB 候选逐文件一致；
包内 `.run` 与云端构建、安装并完成全部验证的二进制一致。S02BA 和历史
包均未覆盖。

## 6. 官方评测要求

请提交第 5 节的 S02BB ZIP，并完整回传：

```text
Case1: Pass, Result: ...
Case2: Pass, Result: ...
Case3: Pass, Result: ...
Case4: Pass, Result: ...
Case5: Pass, Result: ...
prof_sum: ...
```

同时记录提交时间。官方结果的判定顺序：

1. 任一 Case 不正确则立即回退 S02BA 包并定位；
2. 五项正确后，与 S02BA 五个 Case 逐项比较；
3. 不假定 Case4 一定命中宽度 8；只有平台结果能证明实际覆盖；
4. 若总耗时未达到 `1195.599 μs`，继续从仍占主导的 Case 对应通用慢路
   建立单变量候选。

## 7. 当前结论边界

可以确认：S02BB 在公开合法的目标路径上取得 1.84～24.69 倍加速，
控制路径未被修改，全部现有正确性矩阵通过，包身份闭环。

不能确认：S02BB 是否命中官方 Case4、官方 `prof_sum` 会下降多少、是否
达到前十或 `0.30 × T_baseline`。这些结论必须等待本包官方五 Case。
