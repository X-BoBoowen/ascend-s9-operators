# SquareSumV1 S03L 云端性能、正确性与打包门禁

日期：2026-08-07（Asia/Shanghai）

## 1. 结论

S03L 已通过内部提交门禁并生成正式评测包，但尚无比赛平台五 Case 成绩：

- 目标路径 A/B/A：15/15 正确，聚合改善 **89.6064%**；
- 最差目标单点改善：**79.2200%**；
- 两轮目标基线最大漂移：**9.6075%**；
- 未命中新路径的控制组：18/18 正确，最大表观回退 **7.2830%**；
- 控制组两轮基线最大漂移：**5.4654%**；
- 累计合法正确性：**1235/1235**；
- ZIP 结构、源码逐字节一致性和 `.run` 0755 权限审计通过。

以上均为 Ascend 910B、CANN 8.5.0 上的通用公开约束形状，不是隐藏 Case
成绩，不用于猜测隐藏 shape、dtype 或 TilingKey。只有正式五 Case 返回值
能够决定 S03L 是否晋升基线。

## 2. 根因与实现

S03K 正式结果为 `3277.140 μs`，五个 Case 全部比 S03J 慢。重新检查
全路径图谱后发现，rank-5 非连续多轴归约仍存在另一类结构性问题：旧
`fastPath4` 对每个输出行、每个外层归约组分别发起短 DMA。典型公开形状
`[8,64,32,128,16] / axes=[1,3]` 约产生 16,384 次细粒度搬运，基线仅约
32 GB/s。

S03L 新增 tiling key 7，将物理相邻的 2/4/8 个保留输出行合成一次连续
DMA，在 UB 内分别对各行平方、树归约并以 FP32 累加，最后连续写回这些行。
路由只在下列通用条件同时成立时启用：

- `fastPath == 4` 且至少有两个归约轴；
- 输入不少于 1 Mi 元素，归约元素数不少于 2048；
- trailing inner 为 8 的倍数；
- 最内层归约维为大于 1 的 2 次幂；
- 分组后的输入缓冲不超过 8192 元素，输出缓冲不超过 1024 元素；
- 计算出的独立任务数不少于 32。

分组宽度按 8、4、2 依次选择。奇数归约长度、非对齐 inner、任务不足、
UB 容量不足以及其他 fastPath 均保留原实现。

## 3. 目标 A/B/A

基线是独立安装的 S03K，候选是从 S03L 源码全新构建并独立安装的包。
每点取 5 次 NPU Event 中位数，执行顺序为 S03K-1 / S03L / S03K-2。

| 代表形状 | dtype | S03K 中位基线（μs） | S03L（μs） | 改善 |
| --- | --- | ---: | ---: | ---: |
| `[8,64,32,128,16]`, axes `[1,3]` | fp16 | 2066.520 | 169.370 | 91.80% |
| 同上 | bf16 | 2100.970 | 204.830 | 90.25% |
| 同上 | fp32 | 1062.415 | 220.770 | 79.22% |
| `[4,64,64,64,16]`, axes `[1,3]` | fp16 | 1453.210 | 123.890 | 91.48% |
| 同上 | bf16 | 1468.840 | 139.200 | 90.52% |
| 同上 | fp32 | 764.335 | 124.330 | 83.73% |
| `[8,32,32,128,8]`, axes `[1,3]` | fp16 | 1502.990 | 80.190 | 94.66% |
| 同上 | bf16 | 1535.675 | 91.300 | 94.05% |
| 同上 | fp32 | 936.920 | 83.940 | 91.04% |

15 个目标点的基线中位数总和为 `18763.6500 μs`，S03L 为
`1950.2200 μs`，聚合改善 `89.6064%`。全部逐点结果保存在
`candidates/squaresum_s03l_strided_grouped_rows_20260807/evidence/target_result.json`。

## 4. 控制组与正确性

控制组覆盖：最内层归约为 127、inner 为 15、任务数不足、rank-4
fastPath4、fastPath3 和 S03K fastPath2。18 点全部正确。最大表观回退
`7.2830%` 出现在未修改的 BF16 fastPath2 小耗时点
`81.560 -> 87.500 μs`；该点两轮基线自身漂移 `5.0515%`，未观察到
结构性旧路径退化。

| 正确性套件 | 通过数 |
| --- | ---: |
| directed | 46 |
| BF16 语义 | 4 |
| random | 150 |
| extended | 726 |
| S03J 边界 | 135 |
| S03K 边界 | 69 |
| S03L 分组宽度、尾块与阈值边界 | 105 |
| 合计 | **1235/1235** |

S03L 专项覆盖分组维 7/8/9、15/16/17、31/32/33，宽度 8/4/2，
inner 7/8/15/16/24/32/64/128，任务数 31/32/33，输入 1 Mi 门槛前后，
最内层归约 63/64/65/127/128/129/256/512/1024，以及负轴、乱序轴、
`keep_dims` 和 singleton gap。

初版专项脚本曾误放入两个 rank-6 形状，超出题面 rank 1～5 约束，Host
tiling 正确拒绝。它们未计入结果；脚本改为合法 rank-5 singleton-gap
回退形状后从头得到 105/105。该过程没有修改算子实现或放宽断言。

## 5. 构建与提交包

```text
Host SHA-256:
89cea936a3c529509d9f82e68a27f61f7b8ff75eea5397d7f2cee9a28b220a30

Kernel SHA-256:
05b461a51b5afc64430c0b958b3ebe531a3a604ce403e2459218da1e2c174c74

Kernel CMake SHA-256:
fa670d65999abd028adcd67153122364aba6bcaa1437ca7ff28de60d3bdf26c4

RUN SHA-256:
e46a29644b03c70f960533d2e1c2b2118e4e1f746e35fdebab48508d58dc2acd

ZIP SHA-256:
3a61968292fde3b3cc876b4dcac0e585a9bc039683ccd0767ff17522dd158d4f
```

提交文件：

```text
D:\29722\Desktop\GCC\提交相关材料\20260807\S03L\SquareSumV1.zip
```

ZIP 大小 `494527` 字节，含 9 个条目、唯一顶层目录
`SquareSumV1_zip/`，五个源码/CMake 文件与已验证 `.run` 均逐字节一致，
`.run` 权限为 0755。打包辅助脚本同时改为 Python 3.9 兼容的
`Optional[Path]` 注解，以适配 ModelArts 环境；该改动不进入提交 ZIP。

## 6. 决策边界

S03L 是当前唯一推荐送交正式评测的新候选，但在平台结果返回前不得宣称
超过 S02F 或进入前十。若正式结果没有显著降低原先约 1717 μs 的主耗时
项，则说明通用 rank-5 分组域仍未映射到评分主路径，应保留本报告并回到
S02F 做新的通用路径归因；不得靠猜隐藏数据扩大路由。
