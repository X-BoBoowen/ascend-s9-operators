# S9 专家意见与往届代码综合审计

更新时间：2026-07-27
适用环境：CANN 社区版 8.5.0、Ascend 910B4
评判原则：专家意见和往届代码只作为候选思路；最终以 S9 官方题意、PyTorch 参考语义、CANN 8.5.0 接口约束、当前实现及 910B4 实测结果为准。

## 1. 已纳入审计的材料

- 先前专家文档：`check.md`
- 新增专家文档：
  - `fable/docs/S9_五题严格代码评审与优化方案.docx`
  - `fable/docs/S9_算子优化策略详解.docx`
- S9 赛题 Excel、其中的文档链接及已归档网页
- 当前云端五题源码、编译产物、验证脚本与性能记录
- 专家点名的 10 个往届仓库快照：
  - R002 `szdaniel/S6T`
  - R071 `2044145178/AscendC_S4`
  - R075 `ascend-124/s6`
  - R078 `Brown-Bert/S5`
  - R079 `bubu-12/Ascendc-S4`
  - R097 `Qeeweew/AscendC-S6-Logcumsumexp`
  - R104 `ustc1587/ReduceLogSumExp`
  - R107 `winfan1314/AscendC-S4-Base`
  - R108 `wybxw/Ascend_ops_Base_S6`
  - R109 `wybxw/Ascend_ops_Performance_S6`

## 2. 综合结论

专家材料提供了有价值的优化方向，但存在硬件代际混用、对仓库实现的过度概括、把示例代码当通用实现、忽略 PyTorch 逐步舍入语义等问题。不能直接复制任何一份往届代码。

### 2.1 直接采纳并已纳入设计

1. **尾块安全搬运**
   - 非对齐尾块优先使用 CANN 8.5.0 的 `DataCopyPad`，避免越界读写和隐藏用例失败。
   - 适用于五题的 GM↔UB 搬运；仍需按具体数据排布判断是否能连续搬运。

2. **动态核数与负载均衡**
   - Host 侧依据实际任务量和平台核数确定 `blockDim`，不照搬往届代码中的 8、20、40 等硬编码。
   - 小任务避免启动过多核；大任务按输出所有权或连续区间均衡分配。

3. **编译期 TilingKey 分支**
   - Host 通过 `SetTilingKey` 选择路径，Kernel 使用显式 `TILING_KEY_IS(n)` 入口。
   - 已在 IndexAdd 中实机确认：每一个使用的 key 都必须显式出现；仅写普通 `else` 会导致运行时找不到相应入口。

4. **数据类型模板化**
   - 对 fp32、fp16、bf16、int32、int8 分别保证合法指令、转换路径与语义。
   - 仅在语义允许时使用 fp32 中间计算；不能因为“精度更高”就改变参考实现的逐步舍入结果。

5. **输出所有权代替原子竞争**
   - IndexAdd 按目标输出行分组，让同一输出元素只由一个核按源顺序更新。
   - 该方案同时解决跨核写冲突、重复 index 语义和确定性问题，比往届 ScatterReduce 中复杂且风险较高的通用散射方案更适合本题。

### 2.2 有条件采纳

1. **Compare / Select**
   - 这是实现比较、条件选择、NaN 特殊语义的有效工具，不是所有 Greater 路径都必须机械地套用这一组合。
   - 是否使用取决于 dtype、广播形式和 CANN 指令支持，最终由正确性及性能实测决定。

2. **广播展开与块复制**
   - 可借鉴 R108/R109 的重复块复制和分阶段广播思想。
   - 不采纳其 310B 专用 UB 容量、固定核数、最多三阶段等约束；S9 需覆盖正式题目允许的任意合法形状。

3. **归约分层**
   - 可借鉴向量归约、分块归约及 fp16/bf16 中间转换思路。
   - 必须结合 SquareSumV1 的精确输出形状、dtype 和 PyTorch 参考行为重做，不能照搬 R104 的实现。

4. **索引缓存**
   - IndexAdd 将 index 缓存在 UB，可减少重复 GM 访问。
   - 仍需受 UB 容量和 index 上限约束；超过容量时必须安全分段，不能假设固定大小。

### 2.3 明确不采纳

1. 固定 `blockDim=8/20/40` 或假定 310B 的硬件参数。
2. 固定 240 KB UB、固定 rank、固定 shape 数组长度、固定 tile 长度。
3. 以逐元素 `GetValue/SetValue` 作为通用高性能路径。
4. 未实现完的广播分支、`assert(0)` 分支或注释掉核心计算的代码。
5. 带有明显变量拼写错误、越界尾块、重复写输出或错误 InferShape 的实现。
6. 不经验证地把 fp16/bf16 累加统一提升到 fp32；这可能改变 PyTorch 逐次更新的舍入语义。
7. 因某仓库“获奖/排名高”便直接复制，其题目、芯片、CANN 版本和隐藏用例均可能不同。

## 3. 点名仓库逐项结论

| 仓库 | 可借鉴内容 | 风险与最终处理 |
|---|---|---|
| R002 | 动态核数、`DataCopyPad`、模板、Cast、多 TilingKey | 可作为工程模式参考，不复制其 Fmin 语义和路径数量 |
| R071 | `CompareScalar + Select`、多路径、输出分组思路 | 通用广播路径逐元素处理，不作为性能模板 |
| R075 | NaN 语义处理、dtype/广播 TilingKey | Host 广播 InferShape 存疑，只借鉴局部技术 |
| R078 | index 缓存、整行搬运概念 | 310B 专用、固定长度、标量循环及尾块风险，不复制 |
| R079 | fold/stride、非散射维切片搬运 | Host 检查不足且 Kernel 有明显拼写/正确性风险 |
| R097 | last-axis/非连续路径拆分、fp32 状态、`DataCopyPad` | 固定 20 核、单缓冲、标量循环及近似数学，不照搬 |
| R104 | Cast 与 ReduceSum 局部用法 | 固定 rank/核数、InferShape 和多核写入存在问题 |
| R107 | `SetTilingKey` 与 `TILING_KEY_IS` 的规范写法 | 已用于验证 IndexAdd 编译期分支机制 |
| R108 | 重复块复制、广播展开 | 310B、固定 UB、单核、阶段数有限，只取机制 |
| R109 | 模板计算类型、分块向量计算 | 固定 40 核，部分广播 key 未完成，不可直接使用 |

## 4. 当前五题决策

### Concat

- 保持已通过 109 项本地/云端组合验证的实现。
- 重点使用连续区间搬运、尾块安全、动态任务划分。
- 不因专家建议重新引入高复杂度多路径，除非平台成绩显示明确瓶颈。

### Greater

- 保持已通过 246 项组合验证的广播与 dtype 路径。
- Compare/Select 只在对应路径确有收益且语义一致时使用。
- 不采用固定 rank 或不完整广播实现。

### IndexAdd

- 当前方案：任意 rank 1–8、任意/负 dim、五种 dtype；按目标输出行所有权处理重复 index。
- TilingKey 1：稳定的一加一回写路径。
- TilingKey 2：仅对高碰撞 int8 使用两次加法后一次回写，以减少重复读写。
- Host 当前阈值：`indexCount > dimSize * 4` 时选择 int8 批处理路径。
- 已完成 23 项定向、170 项原随机、345 项扩展矩阵，共 538 项通过。
- 910B4 task-time 代表性结果：公开形状约 9.108 μs；高碰撞 int8 约 256.659 μs。稳定路径在高碰撞 int8 约 332.607 μs。

### Transpose

- 已按官方 perm/shape/dtype 范围完成 48 定向 + 200 随机 +
  152 扩展矩阵，共 400 项逐位通过。
- 最终路径包括：恒等/连续段搬运、FP16 16×16 单矩阵、FP16
  批矩阵、循环后缀二维化、四 dtype 尾块二维转置、跨步
  `DataCopyPad + GatherMask` 和保底坐标映射。
- 循环置换 `(32,64,128)` 两个方向约 10.818/9.879 μs；
  任意 rank-4 示例从 1024.657 μs 降至 134.399 μs；
  NCHW↔NHWC（1,048,576 元素）约 16.3 μs。
- 公开 FP16 128×256 约 4.44–4.60 μs；40 核 A/B 为
  5.130 μs，故保留经验证的 32 核。说明动态核数必须实测，
  不能采纳“固定 40 核一定更快”的专家经验。
- 最终包 `提交相关材料/Transpose.zip` 的 SHA-256 为
  `0b88b825e7ee6e5cb598e5c3eb4638d4f652a4a0eb32671087a10a4aa3ff57e8`，
  云端解包结果为 `FINAL_TRANSPOSE_PACKAGE_REMOTE_VERIFY_OK`。

### SquareSumV1

- 后续必须先确认所有 axis、keepdim、dtype、空维/小维语义。
- 优先采用“元素平方 + 分层向量归约 + 尾块安全”的可验证结构。
- fp16/bf16 是否用 fp32 中间值必须以参考结果的容差和逐步语义实测决定。

## 5. 后续验收门槛

每题提交前必须同时满足：

1. 官方算子名、包目录名、注册名完全一致。
2. Host InferShape/InferDataType 与题意一致。
3. 合法 dtype、rank、axis/dim、广播、尾块、重复索引和极小/极大形状全部覆盖。
4. 重新安装最终 `.run` 后执行验证，不能只测试源码目录中的旧产物。
5. 提交 ZIP 解压后再次核对文件清单和 SHA-256。
6. 性能结论优先采用设备 task-time；Event/端到端时间只作辅助。
7. 专家建议只有在正确性不退化、CANN 8.5 合法、910B4 实测更优时才进入最终包。
